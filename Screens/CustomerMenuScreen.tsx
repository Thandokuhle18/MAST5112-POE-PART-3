import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CustomerMenuScreen = () => {
  return (
    <View>
      <Text>CustomerMenuScreen</Text>
    </View>
  );
};

export default CustomerMenuScreen;

const styles = StyleSheet.create({});
